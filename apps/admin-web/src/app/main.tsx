import { createRoot } from 'react-dom/client'

createRoot(document.getElementById('root')!).render(
  <main>
    <h1>Internal Admin Console</h1>
    <p>Tenant support, devices, integrations, feature flags, and sync health.</p>
  </main>,
)

