import { createRoot } from 'react-dom/client'

createRoot(document.getElementById('root')!).render(
  <main>
    <h1>Customer Web</h1>
    <p>QR ordering, online ordering, booking, loyalty, receipts, and feedback.</p>
  </main>,
)
