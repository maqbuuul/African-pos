import { createRoot } from 'react-dom/client'

createRoot(document.getElementById('root')!).render(
  <main>
    <h1>Owner Dashboard</h1>
    <p>Revenue, profit, forecasts, alerts, and AI recommendations.</p>
  </main>,
)

