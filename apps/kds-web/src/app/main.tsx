import { createRoot } from 'react-dom/client'

createRoot(document.getElementById('root')!).render(
  <main>
    <h1>Kitchen Display System</h1>
    <p>Station tickets, timers, item status, and expo workflow.</p>
  </main>,
)

